#include "lte.h"


uint8_t ec20_send_cmd(char *cmd, char *ack, char *ack2,  char *err, uint32_t timeout)
{
/* 
    //把ms转换成系统滴答时间
    TickType_t xMaxBlockTime = pdMS_TO_TICKS(300);
    //获取当前的时间
    
    vTaskSetTimeOutState();
    //与过去的时间比较是否超时
    xTaskCheckForTimeOut();
     */
    while(1)
    {

    }
}

